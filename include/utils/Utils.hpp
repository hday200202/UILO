#pragma once

/*
    Utils.hpp:
    - Desc:     Convenience header pulling in the small value types the layout
                API is
            written in, so an element header can include one thing rather than
            three.
*/

#include "Alignment.hpp"
#include "Dimension.hpp"
#include "Timer.hpp"